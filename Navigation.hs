module Navigation
where

-- Extern
import Text.XHtml.Strict hiding (sub)

--   Blog Navigation
-- -------------------
--   * latest (by date)
--   * this month (title by month this year (only this month))
--   * last month  (title by month this year (only last month))
--   * this year by month
--   * all articles (all by subject)

